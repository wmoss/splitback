## Go
To get the Go code to complile you need to fetch the necessary packages by running smething like:
```
export GOPATH=$(pwd)/splitback
go get code.google.com/p/goauth2/oauth
go get <The gmail API>
```
